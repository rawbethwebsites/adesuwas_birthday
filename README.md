# Adesuwas birthday

A small web page with a letter and envelope animation for Adesuwa's birthday. 

This repository includes:

- `index.html` — page markup
- `styles.css` — styling
- `script.js` — GSAP animations and typing effect

How to push to GitHub (example):

1. Initialize git and commit:

   git init
   git add .
   git commit -m "Initial commit: add project files"

2. Create a GitHub repo named `Adesuwas birthday` (public) via the website or GitHub CLI.

3. Add remote and push (replace URL with your repo's remote):

   git remote add origin https://github.com/<your-username>/Adesuwas-birthday.git
   git branch -M main
   git push -u origin main

If you'd like, I can provide commands tailored to using the `gh` CLI or create the repo via the GitHub API if you provide a personal access token.
